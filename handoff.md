# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
用 grilling 技能完成了五大功能＋新增功能六（NotebookLM 整合）的完整需求訪談，所有核心設計問題都已定案（詳見 `agents.md` 的「功能設計總覽」段落）。這次還額外研究並存檔了「素養導向命題」的官方參考文件。訪談尚未正式宣告結束，還沒開始寫 `SKILL.md`。

## 🚦 目前狀態
需求訪談階段，還沒開始寫任何技能程式碼或 SKILL.md。設計層面目前沒有已知的未解問題，但還沒明確問過使用者「訪談是否已經結束、可以進入撰寫階段」。

## ➡️ 下一步
1. 開工後先問使用者：訪談要不要繼續深入其他細節，還是可以直接進入撰寫 `SKILL.md` 階段。
2. 若要進入撰寫階段：依 `agents.md` 的「功能設計總覽」整理成正式 SKILL.md（含五步驟命題生成流程、三家出版社下載機制、esa-exam-review 交接、NotebookLM 整合、輸出存放規則）。
3. NotebookLM 功能實際要用之前，使用者需要自己執行 `nlm login` 完成 Google OAuth 授權（目前 `auth_status: stale`，Agent 不能代為登入）。

## ⚠️ 注意事項
- **命題生成流程五步驟（教材擷取→建骨架→逐格撰寫→整份查重→格式化輸出）必須完整執行，Agent 不能自行判斷跳過；遇到不確定要暫停詢問使用者，不能自己猜答案繼續做。** 這是使用者明確強調的核心規範。
- **翰林、南一教材下載機制已實測成功**：用「Claude in Chrome」讀取使用者真實 Chrome（deviceId 332bc14d-9c56-4000-9b24-3ca9a51c42b7，暱稱 Browser 1）裡既有的登入狀態，能直接看到課本/習作/教師手冊 PDF 的下載按鈕，不需 Agent 碰觸密碼。使用者電腦上連接了兩個 Chrome，之後每次用 Claude in Chrome 前要先確認選對台。
- **素養導向題型細節**：獨立題型類別（跟選擇題/簡答題並列佔百分比），素養題額外要標情境範疇＋學習表現代碼＋學習內容代碼＋試題概念分析，非選擇題另附評分準則。所有題目寫完都要過一份試題檢核清單（情境真實性/語句通順/資訊完整/問題明確/公平無偏見）才能送查重。詳細原則見新存入的兩份參考文件（`定期命題參考架構/素養導向紙筆測驗要素與範例試題(定稿版).pdf` 與既有的 `數學素養導向評量試題研發策略.docx`）。
- **NotebookLM 整合是新增的功能六**，不在原始五大功能清單裡，是這次訪談中臨時加入的需求，寫 SKILL.md 時不要漏掉。設計是單一累積筆記本（不分次別分開建）＋來源標籤分類＋通用參考資料只加一次。
- 這台電腦（DESKTOP-31QBU95）的 NotebookLM CLI 目前未登入（`auth_status: stale`），涉及 OAuth 授權的服務 Agent 不能代使用者完成登入。

## 🕐 最後更新
- 時間：2026-08-02
- 更新者：Claude Code @ DESKTOP-31QBU95
- Git push：✅ 已推
