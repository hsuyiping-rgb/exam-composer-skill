# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（若有 L3）。

## ⏯️ 目前做到哪
需求訪談結束，已撰寫完整技能 `exam-composer`（`C:\Users\vm\.claude\skills\exam-composer\SKILL.md`），並用真實資料端到端測試了 Step 0～Step 4（範圍：六年級國語115上、康軒版、第1~7課）。測試過程抓到三個真實的設計落差並已修正（見下方注意事項）。功能五（篩選替換）、功能六（NotebookLM 整合）還沒實測。

## 🚦 目前狀態
- 技能本體 SKILL.md：已寫成，且經過 Step 0～4 真實測試調整過一版。
- 專案工作區：`命題成果/115上_六年級_國語_第一次定期評量/設定摘要.md` 是測試產物；`命題範圍三家出版商教材/` 裡有 4 份真實康軒 PDF（約183MB，已加進 .gitignore 不進版控）。
- `光復108課綱歷年定期試題/` 資料夾維持空的——這是正常狀態，不是漏做，Step 4 改成即時讀取 ESA 內容比對，不預先存檔。

## ➡️ 下一步
1. 把 Step 4 的資料夾存放描述在 SKILL.md 裡同步修正措辭（目前只有 agents.md 記錄了「不需要存 PDF 進資料夾」這個發現，SKILL.md 原文字還沒改）。
2. 測試功能五（篩選替換）與功能六（NotebookLM 整合），兩者目前只有設計、沒有實測過。
3. 若要正式讓某位命題教師使用，Step 1 需要把其餘六課（朱子治家格言選、談遇見更好的自己、臺灣美食詩選、最好的味覺禮物、珍珠奶茶、大小剛好的鞋子）也逐頁精讀，測試時只精讀了第一課「跑道」。
4. Step 4 只測過 108-1 一份歷史考卷，正式使用前建議測試涵蓋 108~114 全部年度的比對流程是否順暢。

## ⚠️ 注意事項
- **Step A（出版社教材下載）已修正為半自動**：Agent 只負責導航到正確頁面＋列出下載清單，實際點擊下載一定要使用者親手做（瀏覽器會限制/攔截非使用者親手觸發的下載，這是安全機制，不應設法繞過）。使用者測試時已用這個流程成功下載了康軒真實教材。
- **esa-exam-review 的兩個版本都已修正密碼規則**：`~/.codex/skills/esa-exam-review/scripts/download_esa_history.js`（改成開視覺化瀏覽器視窗，等使用者自己登入）與 Claude Code 外掛版 SKILL.md（改成用 Claude in Chrome 讀取既有登入狀態，不夠再請使用者自己登入）。**這兩個檔案不在本專案 git repo 裡**，如果這台電腦有用 chezmoi 同步 `~/.codex/skills`，記得手動 `chezmoi re-add`；Claude 外掛版那份在桌面版同步資料夾裡，目前沒有已知的同步機制，只能記住已經改過。
- **Step 4 不需要（也無法）把歷史 PDF 存進 `光復108課綱歷年定期試題/` 資料夾**：瀏覽器自動化工具有防資料外洩機制，擋下把檔案內容編碼讀出瀏覽器的動作。正確做法是用 `fetch` 轉 blob URL、`window.open` 開新分頁、截圖＋滾動讀取內容，直接拿內容比對，不留檔案。
- **`exam-composer` 這個技能檔在 `~/.claude/skills/exam-composer/`**，若這台電腦有用 chezmoi 同步 `~/.claude/skills`，記得 `chezmoi re-add ~/.claude/skills/exam-composer/`。
- 測試中意外發現：108-1 六年級國語第一次定期評量歷史考卷裡，有一題直接取材自朱子治家格言原文（「施惠勿念，受恩莫忘」翻譯題），之後真的命題到第二課「朱子治家格言選」時要注意避免出得太像。

## 🕐 最後更新
- 時間：2026-08-02
- 更新者：Claude Code @ DESKTOP-31QBU95
- Git push：✅ 已推
