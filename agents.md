# 定期考察命題技能（專案藍圖）

> 本檔為跨 Agent 通用的專案藍圖（AGENTS.md 開放標準）。任何 Agent 的每個 session 都應先讀本檔＋`handoff.md`。

## 專案簡介
打造國小教師命題輔助工具:設定命題範圍與布隆姆百分比、自動下載出版社教材、產出 Word 試題卷與雙向細目表、支援題目篩選替換。使用者為新北市中和區光復國小校長,技能的使用對象是校內命題教師(校長本人另有 `esa-exam-review` 技能負責審查端,兩者分工不重疊)。

## 關鍵時程
（尚無明確時程，訪談中）

## 目標與路線圖
<!-- 用 checklist 追蹤，收工技能會更新這裡 -->
- [x] 需求訪談（grilling）第一階段：確認與 esa-exam-review 的分工（本技能只做生成端，審查端另有 esa-exam-review）
- [x] 需求訪談：出版社教材下載可行性實測（康軒／翰林／南一）——全部驗證成功，見「同步層級」下方的功能總覽
- [x] 需求訪談：翰林、南一登入方式決策——採用「Claude in Chrome」讀取使用者真實瀏覽器既有登入狀態，已實測成功
- [x] 需求訪談：功能一（命題範圍/單元/題型/布隆姆百分比設定）介面——對話輸入＋最後存檔設定摘要
- [x] 需求訪談：功能三與 esa-exam-review 的交接介面——整合進命題生成流程 Step 4（整份送查重，重複題只重寫該格，直到全過）
- [x] 需求訪談：功能四（Word 試題＋雙向細目表）產出流程與題目來源——AI 直接生成全新題目，五步驟流程定案，含素養導向題型與試題檢核清單
- [x] 需求訪談：功能五（題目篩選與替換）互動方式——對話指定題號，依原規格重新出題並查重後直接替換
- [x] 需求訪談：新增功能六（命題成果自動整理進 NotebookLM 筆記本）——每一次命題任務建立單獨 Notebook，不與其他任務混用
- [x] 需求修正：命題時若題目文字需要圖像輔助，需依課本/習作圖示作為構圖參考，必要時召喚 `draw` 技能產生或改版圖像
- [x] 訪談結論定稿，撰寫技能 SKILL.md（`C:\Users\vm\.claude\skills\exam-composer\SKILL.md`，觸發詞：命題/出考卷/定期考察命題/設計試題/生成試題卷/雙向細目表/幫我出題）
- [x] 端到端測試 Step 0～Step 4（用真實康軒六年級國語115上課本，範圍第1~7課）——詳見下方「測試發現與修正」
- [x] 端到端測試 Step 5（格式化輸出）＋功能五（篩選替換）＋功能六（NotebookLM 整合）——詳見下方「測試發現與修正」第 5-6 點
- [x] Step 4 查重覆蓋度驗證：2026-08-04 六年級社會命題任務首次把 108~114 共 7 個年度全部實際跑過一輪（見下方測試發現第 9 點）
- [ ] 正式上線前，需要把 Step 3 對其餘六課（朱子治家格言選、談遇見更好的自己、臺灣美食詩選、最好的味覺禮物、珍珠奶茶、大小剛好的鞋子）也逐頁精讀後才能真的產出完整試卷（測試時只精讀了第一課「跑道」，這是115上六年級**國語**任務，跟社會任務不同科目）
- [x] SKILL.md 已同步 Step 4「不需要存 PDF 進資料夾」修正，並補上 Step 0/Step A 完整流程、NotebookLM 標籤副作用、Step 5 中文路徑防呆、正式出卷前檢查
- [x] 2026-08-04：Step A 改版——教材下載由「使用者全程親手點擊」改為「Agent 引導使用者完成登入後，Agent 接手點擊下載並逐檔驗證是否落地，失敗才請使用者補下載」（見下方測試發現第 10 點）
- [x] 2026-09-03：`exam-composer` 技能檔納入本 repo 版控（`skills/exam-composer/SKILL.md`），解決換電腦後找不到技能來源的問題（見下方測試發現第 12 點）
- [x] 2026-09-04：Step A 新流程（Agent 接手下載）**實測成功結案**，翰林五上數學教材 31 份已取得；同步更正第 13 點對翰林入口的錯誤結論（見第 14 點）
- [x] 2026-09-04：回到舊機器 DESKTOP-31QBU95，取回原版 SKILL.md 與 repo 重建版**逐段比對合併完成**，四份 SHA-256 對齊，技能檔版本分裂問題結案（見第 15 點）
- [x] 2026-09-07：**五年級數學任務 Step 4 查重完成並結案**——108~114 共 7 個學年度逐份實際開啟比對，判定 10 題接近原題重複並全數重寫、通過第二輪查重；Step 5 三份 Word 同步更新（見第 16 點）
- [x] 2026-09-07：**Step 4 實戰經驗回寫進 SKILL.md**（337→409 行）——新增 Step 3「撰寫前先避開歷屆高頻命題框架」對照表、Step 4 四級判定與重寫三條硬約束、ESA 操作要點、Step 5 .docx 就地編輯法、環境需求章節；四份 SHA-256 對齊為 `371CD35F…B61B55CB`
- [x] 2026-09-07：**115上_五年級_數學_第一次定期考察 全案結案並經使用者確認驗收**（DESKTOP-31QBU95）——補寫 L3 Obsidian 筆記、補做 Step 5 視覺渲染 QA（LibreOffice 轉 PDF 逐頁檢視三份 Word 皆通過）、刪除 Step 4 前備份、完成功能六 NotebookLM 歸檔（筆記本 `3c7df9ba-71cf-4d12-b6a1-84b7272c6479`，47 份來源）；同步更正 handoff.md「三台都沒有 LibreOffice」的錯誤敘述（見第 18 點）
- [x] 2026-09-07：**`exam-composer` 轉換為 Antigravity 全域技能並完成五處 SHA-256 對齊**——適配 Antigravity 原生生圖（`generate_image`）、PowerShell 指令、LibreOffice 轉檔與完整命題流程，安裝至 `C:\Users\vm\.gemini\config\skills\exam-composer\SKILL.md`；同步更新 repo 與本機 Claude、Codex、Agents 三處技能複本，五份 SHA-256 對齊為 `B7D1549BCA4DDC0C488C0AC967147ED92E850E721539D3B00CC3922DA5FC0AD8`
- [x] 2026-09-07：**115上_二年級_國語_第一次定期考察 命題完成並產出 Word 卷與 QA 通過**（Antigravity）——依光復選用表取得康軒二上教材 4 包整冊 ZIP（87 個 PDF），精讀查字典與第 1～6 課（96 生字與課綱代碼），依 100 分細目骨架全新命製 57 計分項試題；生成 2 幅手繪素養插圖；依指示先跳過查重；Step 5 產出三份 Word（正式試題卷、教師解答卷、雙向細目表）及 PDF，通過結構性與 LibreOffice 視覺渲染雙重 QA
- [x] 2026-09-07：**115上_五年級_國語_第一次定期考察 Step 0～Step 5 全流程完成**（DESKTOP-31QBU95）——Step 3 撰寫 38 小題／100 分（非選 46 分，依命題教師要求提高非選比重）；Step 4 查重 108～114 共 7 個學年度**全數取得並實際判讀**（109-1 為 OLE2 二進位 `.doc`，改以 UTF-16LE 在瀏覽器內解析出全文與答案，無缺口），判定 ★6／▲8／△10／－14，經命題教師決議只改寫 ★ 6 題並通過第二輪查重；Step 5 產出三份 Word 並完成結構性 QA 與 LibreOffice 視覺渲染 QA。**本次查到的五上國語高頻命題框架已寫入 `查重報告.md` 第二節**（短文挖空寫國字、改錯字、成語代碼填空、選擇題大題四種慣用大題，及動畫大師／運動員／齊柏林／凱瑟琳四類高頻題材）
- [ ] 未來每個年段／科目任務，把該次查到的高頻命題框架回寫進 SKILL.md 的對照表（目前僅有五上數學實證資料）

## 測試發現與修正（2026-08-02，用真實資料端到端測試 Step 0～Step 4）

1. **Step A 修正為半自動**：原設計以為康軒授權深連結／Claude in Chrome 讀取既有登入狀態可以讓 Agent 直接觸發下載，實測發現只能「看到清單」不能「真的觸發下載」（瀏覽器會限制/攔截非使用者親手觸發的下載，不應設法繞過）。已修正 SKILL.md：Agent 負責導航＋列清單，使用者親手點擊下載，Agent 協助歸檔。使用者實測後成功下載了康軒六上國語課本/習作/教冊資料篇/教學篇共 4 個真實檔案（約 183MB），存進 `命題範圍三家出版商教材/`。
2. **esa-exam-review 的密碼規則修正**：發現 esa-exam-review 兩個版本（`~/.codex/skills/esa-exam-review` 的下載腳本、Claude Code 外掛版 SKILL.md）都要求 Agent 親自輸入 ESA 帳密，跟本專案「Agent 不得輸入密碼」的規則衝突。已修正兩邊：改成開視窗/用 Claude in Chrome 讓使用者親自登入，Agent 只等待、不輸入。**這兩個修正檔案不在本專案 git repo 內**（分別在 `~/.codex/skills/` 與 Claude 桌面版外掛同步資料夾），若有 chezmoi 同步 `.codex/skills`，記得手動 `chezmoi re-add`。
3. **Step 4 的資料存放方式修正**：原設計以為要把歷史考卷 PDF 下載存進 `光復108課綱歷年定期試題/` 資料夾。實測發現瀏覽器自動化工具有「防止資料外洩」的防護機制，會擋下把檔案內容以編碼文字方式讀出瀏覽器的動作，導致無法把 ESA 下載的 PDF 存成本機檔案。改用 esa-exam-review 原本就設計好的方法：**用 `fetch` 在瀏覽器裡把 PDF 轉成 blob URL、`window.open` 開新分頁、用截圖＋滾動的方式讀取內容**，直接拿讀到的內容做比對，不需要也不能預先囤積歷史 PDF 檔案在資料夾裡。`光復108課綱歷年定期試題/` 資料夾目前仍是空的，且**不需要真的存 PDF 進去**——這點跟原本規劃不同，SKILL.md 的 Step 4 描述需要在下次調整時同步修正措辭（目前 SKILL.md 文字還沒改，只有 agents.md 記錄了這個發現）。
4. **Step 4 實測比對結果**：抓到 108學年上、六年級、國語、第一次定期評量、康軒版、範圍「第一課至統整活動二」的真實歷史考卷（1份，4頁，esa 校代碼 014796），逐頁截圖讀完，跟測試撰寫的 3 格題目（國字注音「軸」、造句「奪」、素養題「政彬/名揚接力賽團隊合作」）比對，結果為「未發現相同題」。但發現兩個有價值的參考：(a) 該考卷有一題「運動家的精神」文章，主題與跑道的體育競賽精神相近但非同文本，屬於「題材相近但非同題」；(b) 該考卷有一題「施惠勿念，受恩莫忘」白話文翻譯，正是取材自朱子治家格言原文，跟骨架裡分配給第二課的成語選擇格相關，之後真的寫那一格時要注意避免出得太像。
5. **Step 5（格式化輸出）與功能五（篩選替換）實測成功，但踩到一個腳本層級的低階錯誤**：用 `docx` npm 套件產出了測試版 Word 試題卷＋教師解答卷（3題），送給使用者確認排版無誤。測功能五替換第1題與第3題(1)子題時，重新產生的檔案內容一直沒有真的更新，一度誤判是 Word 檔案鎖定或 Google 雲端硬碟同步問題，最後才查出真正原因：腳本裡用 `OUT_DIR + '\測試試題卷.docx'` 字串相加，中文檔名前的反斜線少打成單一個，JavaScript 把無法識別的跳脫字元（`\測`）直接吃掉反斜線，導致路徑少了資料夾分隔符號，檔案根本沒寫到正確位置（另外還在專案根目錄留下兩個把整個路徑字串串成單一亂碼檔名的垃圾檔案，已清除）。**教訓：以後用 Node.js 组合含中文字元的檔案路徑，一律用 `path.join()`，不要用字串相加＋反斜線，這種錯誤不會拋出例外，非常難排查。**
6. **功能六（NotebookLM 整合）實測成功，後續規則已修正**：`nlm login` 順利完成（帳號 hsuyiping@gmail.com 已有現成 Google 登入狀態，走 OAuth 很快）。早期測試曾採「命題成果筆記本」共用筆記本＋標籤分類，並加入通用參考資料與測試來源。2026-08-02 使用者明確修正需求：**每一次命題任務最後都要建立單獨的新 NotebookLM 筆記本，不與其他任務或舊筆記本混用**；通用參考資料與本次教材/成果都加入該任務專屬筆記本。早期實測另發現：呼叫 `label(action=list)` 會觸發 NotebookLM 自己的 AI 自動分類副作用，且沒有可靠的「從標籤移除」操作，以後應避免不必要呼叫 `label list`。
7. **2026-08-02 六年級自然科定期考察命題與高品質科學插圖整合**：為解決自然科試題中「看圖回答」題目缺乏圖片或圖片品質不佳的問題，導入了 PDF 原圖提取與 Pillow 智慧剪裁/重繪覆蓋功能。先從教材 PDF 中提取原版高品質點陣圖，再透過 Pillow 進行局部的背景與舊字擦除，並以微軟正黑體重新在原圖上疊加標註與試題文字 100% 精準吻合的 A、B、C 標記與 (甲)、(乙)、(丙) 答題框，再自動插入 Word 文件中。此技術既保留了原版課本的美術水準與熟悉感，又兼顧了出題的科學精準度，最後將產出成果（包含這 6 幅插圖與對應 word/pdf）成功上傳至 NotebookLM 歸檔標記。
8. **2026-08-02 已將圖像題規則寫入 exam-composer SKILL.md**：逐題撰寫時需依題目文字判斷是否需要圖像；若課本、習作或教師手冊已有相關圖示，先作為構圖與學科精準度參考並可裁切/改標；若題目需要全新情境圖、示意圖、實驗圖、幾何圖、座標圖或改版教材圖，必須召喚全域 `draw` 技能生圖或改圖，輸出到該次 `命題成果/.../images/`，並在插入 Word 前檢查圖中文字、標籤、比例與題幹一致。
9. **2026-08-04 六年級社會科命題，Step 4 首次完整跑完 108~114 全部 7 個年度**：完成正式命題成果 `命題成果/115上_六年級_社會_第一次定期考察/`（翰林版第一~二單元，5課全數逐頁精讀，選擇20/是非10/填充5/素養題組2組，共100分）。Step A 誤判翰林教專 CH 檔案是按「課」拆分，實際是按「單元」拆分（CH1=單元1、CH2=單元2、CH3=單元3...），導致多下載了 CH3~CH5（第三、四單元，本次用不到），已在下載清單中標註更正，供以後判斷檔案結構時參考：先開檔案第一頁確認範圍，不要只憑檔名猜。Step 4 逐一開啟 108-1~114-1 共7份試卷比對，發現 113-1 第22題（記帳表＋建議刪減支出達成儲蓄目標）與本次素養題組①小題2命題框架相近，已改寫小題2為「依表格資訊計算存款所需月數」，改變命題角度後查重通過。Step 5 因本機無 LibreOffice/pandoc，改用 unzip+grep 對 `word/document.xml` 做結構性 QA（比對題數、答案數、表格數），驗證通過但未做視覺排版渲染確認。
10. **2026-08-04 Step A 政策修正**：使用者在任務結束後要求把「三家出版社教材下載」的分工重新劃分——登入仍一律由使用者本人完成（Agent 絕不輸入密碼，核心原則不變），但登入完成後的「切單元頁面、點擊下載按鈕」改由 Agent 接手執行，每次點擊後都要比對下載資料夾確認檔案真的落地才算成功，失敗才請使用者親手補下載。已同步更新 Claude／Codex／Antigravity 三處 SKILL.md（SHA-256 一致）。**這個修正尚未經過真實下載動作端到端驗證**（本次社會任務的教材下載仍是舊流程跑的），下次命題任務執行 Step A 時，就是這個新流程的首次實測，要留意康軒/翰林/南一的下載按鈕在 Agent 點擊後是否真的觸發瀏覽器下載、還是又會被導向登入頁或被攔截，並把結果記錄回這裡。
11. **2026-08-04 瀏覽器連線穩定性踩坑**：這次任務過程中，使用者關閉 Chrome 視窗導致 Claude in Chrome 擴充功能連線中斷、已登入的 ESA session 全部遺失，反覆發生「畫面顯示登出→請使用者重新登入→又斷線」的迴圈，浪費不少來回。**教訓：跟使用者確認 Chrome 視窗全程不要關閉／不要讓電腦休眠，若連線意外中斷，第一件事是呼叫 `list_connected_browsers` 確認目前偵測到的裝置是否是原本那個，不要預設還是同一個連線。**

12. **2026-09-03 技能檔遺失與版控修正**：換到新電腦（`C:\Users\kfes`）開工時發現 `exam-composer` 完全裝不回來——`~/.claude`／`~/.codex`／`~/.agents` 三處皆無、整個 GDrive 全碟搜尋 0 筆、Downloads／Documents／Desktop／OneDrive／chezmoi source 皆無、`git log --all --diff-filter=A` 確認 repo 全歷史從未有過 SKILL.md、GitHub 上 `git ls-remote` 只有 master 單一 ref 且與本地同 commit。**唯一原版只存在舊機器 DESKTOP-31QBU95 的 `C:\Users\vm\.claude\skills\exam-composer\`（SHA-256 `E44025BE…FD0F43`），沒有任何雲端副本。** 根本原因：技能本體從一開始就只放本機路徑、靠人工同步三處，從未進版控。已依本檔「功能設計總覽」與本節 11 點測試發現重建一份 SKILL.md（236 行，SHA-256 `CA6C9C90…6B9368B`，與原版不同，檔頭已標註來源與版本關係），安裝到本機三處並 commit 進 repo `skills/exam-composer/SKILL.md`。**教訓：任何跨機器使用的技能檔都必須進版控，不能只靠 chezmoi 或人工複製。** 若日後從舊機器取回原版，需與重建版逐段比對差異後擇一為準，不要無腦覆蓋。
13. **2026-09-03 Step A 新流程首次實測（翰林／五年級數學）——部分成功，關鍵環節受阻**：在**預覽列內建瀏覽器**（非 Claude in Chrome，該擴充功能在新電腦 kfes 上未安裝，`list_connected_browsers` 回傳空陣列）執行。**成功的部分**：使用者本人在預覽列完成親師生平台登入後，Agent 可正常接手操作——從 `/private/myfavorites` API 取得翰林的 SSO 深連結（`service_type: openid_connect`，`sso_url: https://moe.ntpc.edu.tw/hanlin/`），導航後 OIDC 直接通過、落在 `edisc3.hle.com.tw`「翰林行動大師3」且為已登入狀態；Agent 也能正常操作頁面內元件（學制／科目／學年度／年級四個下拉、電腦版／線上版分頁切換、開啟卡片的 `●●●` 詳細面板）。**受阻的部分**：翰林這條路徑**根本不提供 PDF**，只有兩種交付形式，兩種在預覽列瀏覽器都走不通——(a)「電腦版」的「下載」實際是**光碟 ISO 映像檔**（API `queryBookByCategory2023ISO` 回傳 `https://hle.com/115翰林數學5上電子教科書-1.iso` 等 4 個檔），需掛載並搭配行動大師桌面程式；Agent 點擊「下載」後**未發出任何下載請求**，下載資料夾檔案數維持 34 不變、無 `.crdownload` 暫存檔。(b)「線上版」的「開啟」是 H5 網頁閱讀器（`h5.hle.com.tw/books/edisc/1151/e/<book_code>/PC/index.html`），靠彈出新視窗開啟；在預覽列中 Agent 以座標點擊與程式化 `el.click()` 兩種方式都**未觸發 `window.open`**（已 hook 驗證，攔截陣列為空），新分頁始終沒出現；直接貼閱讀器網址則被伺服器擋下，回應「疑似不正常登入，請您至平台重新登入」——閱讀器需由平台帶 token 開啟。**結論與待驗**：本次無法判定新流程在**真實 Chrome ＋ Claude in Chrome 擴充功能**下是否可行，因為受阻點是預覽列瀏覽器的彈窗行為，不是流程設計本身。下次在有裝擴充功能的機器上要重測一次才能結案。另外要查清楚：2026-08-04 社會科拿到的翰林**課本／習作／教師專用課本 PDF** 究竟來自哪個入口——確定不是行動大師這條路，可能是翰林另一個教師專屬平台，這點目前只有使用者知道，已詢問。
    - **【2026-09-04 更正】本點中「翰林不提供 PDF」「`教材下載清單.md` 記載有誤」的結論是錯的，已由第 14 點推翻。** PDF 一直都在 edisc3，位於**左側選單「教材資源／PPT」**（頁面 `gwty_v2023.html`）。當時找不到的真正原因是**預覽列瀏覽器視窗太窄，整條左側選單沒有渲染出來**，而我只看到「尋找資源→電子書」那一區（那區確實只有 ISO 與 H5）。教訓：在窄視窗裡找不到某個功能時，先確認是不是 RWD 把導覽收起來了，不要直接下「這個平台沒有這個功能」的結論。

14. **2026-09-04 Step A 新流程實測成功結案（翰林／五年級數學）**：在裝好 Claude in Chrome 擴充功能的真實 Chrome 上重跑，**「使用者登入 → Agent 接手點擊下載 → 逐檔驗證落地」全流程驗證通過**，共取得 31 個 PDF、約 337MB，歸檔於 `命題範圍三家出版商教材/翰林/115上_五年級_數學/`（課本6／習作4／習作教用版4／教師專用課本7／教師手冊10）。過程中的五個關鍵發現：(a) **擴充功能要「側邊面板打開」才會建立連線**——只安裝並授予網站權限、但沒點開面板時，`list_connected_browsers` 一直回傳空陣列，一度誤判為「沒登入」。(b) **Agent 程式化點擊確實能觸發真實下載**，檔案完整落地（驗證檔頭 `%PDF-1.6`、檔尾 `%%EOF`、大小與網頁標示吻合）——這是 8/4 改版以來一直未驗證的核心疑問，現已確認可行。(c) **Chrome 會靜默封鎖同一網站的第 2 個以後的自動下載**：第一個成功後連續 5 次點擊全部無聲失敗、下載夾無新檔也無錯誤訊息。需使用者到網站設定把「自動下載」改為允許（屬變更瀏覽器設定，須由使用者本人操作）。(d) **改用網站自己的「勾選＋下載已勾選項目」更可靠**：伺服器打包成單一 ZIP，只觸發一次下載動作，天然避開 Chrome 的多檔封鎖，建議列為 Step A 首選做法。(e) **落地檔案會以 GUID `.tmp` 停留一段時間才更名**，內容其實已完整；驗證方式為「大小連續兩次相同＋檔頭魔數＋`zipfile.testzip()`」。ZIP 內檔名為 Big5，需 `encode('cp437').decode('big5')` 還原，並移除 `(115f233421)` 之類流水號尾碼。
    另補一條教材結構經驗：**翰林數學是按「課」拆檔（L01~L10 即單元一~十），與社會科教專按「單元」包裹不同**——不能把某一科的拆分邏輯套到另一科，一律先下載目次確認。本次即因先讀目次，正確排除了含超範圍單元五的「學習加油讚(一)」，沒有重蹈社會科誤下載 CH3~CH5 的覆轍。


15. **2026-09-04 SKILL.md 兩版合併結案**：回到舊機器 DESKTOP-31QBU95，取回原版（156 行，SHA-256 `E44025BE…FD0F43`），與 repo 重建版（236 行，`CA6C9C90…6B9368B`）逐段比對後合併為 306 行新版，**四份（repo ＋ `~/.claude`／`~/.codex`／`~/.agents`）SHA-256 均為 `A43988F46585F59F1F88A1796444B898E23C89DD7508F07EE7B8059076C73F0A`**。
    - **原版獨有、重建版沒有而補回的內容**：工作區目錄結構與首次執行自動補建；Step A 三家出版社的實際導航網址與康軒授權深連結格式（`945cloud.knsh.com.tw?code=…&series=…` → `digitalmaster.knsh.com.tw#year=…&item=teach`、`edisc3.hle.com.tw`、`nanibox3.oneclass.com.tw/resource`）；偵測到多個瀏覽器時先請使用者選擇；下載前先核對既有教材是否已覆蓋範圍；線上閱讀器標記為「僅線上瀏覽，無法下載」；最後把「自動下載成功」與「需親手補下載」分開列給使用者；`draw` 技能的實際命令列與提示詞六項要求；圖檔路徑與題號對照表；Step 4「把草稿當成 esa-exam-review 原本會審查的『已上傳考卷』來源」的呼叫方式；「正式出卷前檢查」整節（Step 1 精讀覆蓋／Step 4 年度覆蓋／Step 5 三份一致性）；Step 5 輸出後檢查檔案時間與大小；文末參考文件逐項用途註解。
    - **重建版獨有、全部保留的內容**：Step 0 條件表；絕對原則的繁中／不得取用出版社題庫（避免安親班撞題）／不得繞過下載保護；Step 1 逐頁精讀要求；PIL 高品質改圖做法與 `extracted_images/`；「圖像需求評估」段落要求；Step 4 blob URL ＋ 內嵌 iframe 與三類判定；Step 5 結構性／視覺渲染 QA；輸出目錄結構；既有筆記本表；收尾約定。
    - **順帶修正一處會誤導後續 Agent 的過期敘述**：重建版 Step A 仍寫「⚠️ 此新流程尚未經過真實端到端驗證」，已依第 14 點改寫為實測結案，並把 9/4 的五個關鍵發現（側邊面板才建連線、Chrome 靜默封鎖第 2 個以後的自動下載、「勾選＋打包 ZIP」列為首選、GUID `.tmp` 落地驗證法、ZIP 內 Big5 檔名還原）與翰林 PDF 正確入口（左側選單「教材資源／PPT」`gwty_v2023.html`）寫進「已知陷阱」。
    - 另把 `draw.py` 的硬寫死路徑（原版寫 `C:/Users/vm/.Codex/skills/draw/draw.py`，該路徑在本機實際不存在，`draw.py` 位於 `~/.claude/skills/draw/`）改為「先確認實際存在的那一份再呼叫」，避免換機器又踩空。

16. **2026-09-04 Step 5 視覺渲染 QA 在 DESKTOP-31QBU95 上確認可行**：本機有 **LibreOffice 26.8.0.3**（`C:\Program Files\LibreOffice\program\`，winget 安裝），**沒有 pandoc**（pandoc 不在這條管線上，產檔用 `docx` npm、排版檢查用 LibreOffice 轉 PDF，pandoc 無法替代，不需補裝）。拿社會科 `正式試題卷.docx` 實測轉檔：5 頁、448KB、第 1 頁抽得到 1147 字，渲染成圖後標題、表格、題號、選項編號與中文全部正常。**社會科當初標註的「視覺渲染 QA 未完成」是舊機器狀態，在這台可以補做**；五年級數學 Step 5 不必再標註此限制。兩個踩到的陷阱已寫進 SKILL.md Step 5 QA 段落：(a) **必須用 `soffice.com` 不能用 `soffice.exe`**——`.exe` 會立刻脫離回傳（0 秒、無輸出檔），`.com` 才會同步等待（5 頁試卷約 12 秒）；(b) **不能用 exit code 判定成敗**——`.com` 實測回傳 `exit=1` 但 PDF 已完整產出，要改看「輸出檔存在＋頁數＋抽得到文字＋渲染圖目視無誤」。另注意 LibreOffice 字型替換與 Word 不同，渲染只是排版正確性的近似檢查，足以抓跑版／缺字／表格爆框／圖片錯位，仍建議教師用 Word 再開一次。

17. **2026-09-07 五年級數學 Step 4 查重執行與「歷屆撞題率過高」的結構性發現**：在第三台電腦（`DESKTOP-HJA3024`，`C:\Users\user`）執行，7 個學年度全數取得並逐頁判讀。**結果遠比前幾次嚴重：27 題中有 18 題與歷屆相似**（★ 接近原題重複 10 題、▲ 命題框架高度相似 8 題），沒有任何一題是「未發現相似」。經與命題教師確認後採「只改 ★ 的 10 題、改寫時換命題角度而非換數字」，10 題全數重寫並通過第二輪查重。
    - **結構性發現（對本技能的意義）**：光復五上數學歷年命題高度集中在同一批標準題型——「由乘法/除法算式判斷因數倍數敘述」**連續五個學年度**出現（108/109/110/112/114）、「三邊長能否圍成三角形」**五個學年度**出現、「依統計表完成折線圖」兩年出現、109-1 三3 與 111-1 五2 甚至是**完全相同的一題在兩個年度重複使用**。這代表 Step 3 若只依教材與教師手冊撰寫「標準題型」，撞題幾乎是必然。**建議 Step 3 撰寫時就預先避開這些高頻框架**，而不是等到 Step 4 才大量重寫（本次重寫 10 題的成本遠高於一開始就換角度）。
    - **▲ 類的處理原則需要明文化**：SKILL.md 目前寫「完全相同／命題框架相近」兩類都要重寫。但實務上 ▲ 常是課綱核心指標（內角和求角、三角形邊長關係），年年必考，全部迴避會傷內容效度。本次採用的折衷是：**★ 必改、▲ 由命題教師決定並在查重報告中逐題記錄相似處**，建議把這個判準寫進 SKILL.md。
    - **ESA 操作技術要點（已寫進 `查重報告.md` 第七節）**：(a) 試卷清單是 **div-based 版面**（`div.row` ＋ `div.column`），不能用 `querySelectorAll('tr')` 解析；(b) 可用 `POST /jsp/c_exammgt/ExamMgtAction.do` 帶 `method=examMain&mod=examMain&selseyear=<代碼>` 一次取回整個學年期清單，不必操作下拉選單；(c) PDF 檔名在 `div[id^=row2_itmfile_]` 的 `no` 屬性，路徑為 `/central/014796/upfile/exammgt/<檔名>`；(d) **⚠️ 不要直接導航到模組 JSP 網址 `/jsp/c_exammgt/index.jsp?pid=0261`——繞過入口框架會讓伺服器回 `index_error.jsp?err=7` 並作廢整個 session**，本次因此中斷一次要求使用者重新登入，正確作法是登入後只用 `fetch` 取資料、不 navigate；(e) 部分年度試卷檔是 `.doc`（本次 110-1）無法在瀏覽器渲染，改讀**解答卷 PDF**（掃描版含完整題目）即可比對；(f) 執行 JS 時回傳值若含長檔名或編碼字串會被防資料外洩機制擋下（回 `BLOCKED: Cookie/query string data`），要把檔名留在頁面變數內、只回傳短資訊。
    - **Step 5 產出物可就地編輯，不必整份重生**：本次用 **python-docx** 直接改寫既有 `.docx` 的段落文字、替換第 19 題圖片的 blob 與 `a:ext`／`wp:extent` 尺寸，完整保留原排版與範本格式，比重跑產生腳本安全。（本機無 Node `docx` 套件，但有 python-docx 1.2.0。）
    - **重寫若守住題型／Bloom／單元，雙向細目表無須修改**：本次 10 題全部維持原格位，`雙向細目表.docx` 一個字都沒動就仍然正確，這點應列為 Step 4 重寫的硬約束。
    - **視覺渲染 QA 連續第三台電腦無法執行**：DESKTOP-31QBU95、kfes、DESKTOP-HJA3024 都沒有 LibreOffice。建議把「安裝 LibreOffice」列為使用這個技能的環境需求，否則每次都只能做結構性 QA。

18. **2026-09-07 補記（DESKTOP-31QBU95）：兩則資料錯誤的更正，以及 Step 5 視覺渲染 QA 補做完成**：
    - **編號更正**：本節原本有**兩個「第 16 點」**（2026-09-04 LibreOffice、2026-09-07 查重），是 09-07 多 Agent 併行事故的殘留。已把後者改為第 17 點。日後引用測試發現時請連日期一起寫，不要只說「第 N 點」。
    - **handoff.md 的「三台電腦都沒有 LibreOffice」是錯的**，已更正。DESKTOP-31QBU95 早在 2026-09-04 就裝了 LibreOffice 26.8.0.3（即第 16 點所記），沒有的是 kfes 與 DESKTOP-HJA3024。這個錯誤讓「視覺渲染 QA」與「安裝 LibreOffice」在交接檔裡多掛了三天。**教訓：跨機器的環境結論要寫成「哪台有／哪台沒有」，不要寫成「都沒有」**，否則換一台就變成假資訊。
    - **Step 5 視覺渲染 QA 已補做並通過**：`soffice.com --headless --convert-to pdf` 轉出三份（正式試題卷 7 頁／教師解答卷 3 頁／雙向細目表 2 頁），逐頁渲染為圖檢視，標題、表格、7 幅圖、作圖區皆正常，無跑版缺字；細目表橫縱合計皆 100；解答卷答案與重寫後題目一致（第 19 題 ∠B=65°／∠C=115° 對應新的平行四邊形圖），且第 20、21、26 題明訂列舉法，守住「五年級不用短除法」的課綱硬約束。留給命題教師判斷的兩點（第 22 題折線圖已預先畫兩點、版面留白偏多）記在 handoff.md。
    - **另一個踩坑**：用 Bash heredoc 寫含 Windows 路徑的 Python 時，**這層工具會把 `\\` 收斂成 `\`**，所以「多寫一個反斜線跳脫」這招無效，仍會噴 `SyntaxError: (unicode error) ... truncated \UXXXXXXXX escape`。正解是改用 Write 工具直接產生 .py 檔，或把長文另存資料檔讓 Python 讀。

## 功能設計總覽（訪談結論，撰寫 SKILL.md 前的依據）

1. **命題範圍設定**：老師用自然對話輸入範圍/單元/題型%/布隆姆%，最後產出設定摘要存檔。
2. **出版社教材下載**（入口：新北**親師生平台** pts.ntpc.edu.tw，非新北校園通）——**引導使用者登入後，由 Agent 接手下載**：
   - 登入動作一律由使用者本人在畫面上完成（Agent 絕不輸入帳號密碼）。登入完成後，換單元頁面、點擊「單檔下載」等後續操作屬於操作既有登入 session，改由 Agent 接手執行。
   - **2026-08-04 修正後流程**：Agent 導航到正確單元頁面 → 若偵測到未登入就停下來請使用者本人完成登入 → 使用者確認登入後 Agent 整理清單並逐一點擊下載 → 每次點擊後比對下載資料夾確認新檔案真的落地 → 落地失敗才請使用者親手補下載該檔案 → Agent 協助把全部檔案搬到 `命題範圍三家出版商教材/`。**此流程尚未經過真實端到端驗證**，下次命題任務執行時是首次實測，結果要記錄進「測試發現與修正」。
   - 純線上「免下載」電子書不強求下載。
3. **歷屆考古題查重**：不自己做，整合進生成流程 Step 4，交給既有 `esa-exam-review` 技能執行。
4. **生成 Word 試題卷＋雙向細目表**：題目來源＝AI 直接依教材內容撰寫全新題目（不用出版社自帶題庫如康軒「新出題高手」，避免與安親班撞題）。五步驟流程，**必須完整執行、不能自行跳過，遇到不確定要暫停詢問使用者**：
   1. 教材內容擷取（依範圍篩出對應課本/習作/備課用書內容）
   2. 建雙向細目表骨架（題型%×布隆姆%格子表，**題型清單含「素養題」獨立類別**，每格對應課次知識點）
   3. 逐格撰寫題目（依命題原則文件撰寫，附答案/課次/學習內容代碼；**素養題額外標註情境範疇、學習表現代碼、試題概念與分析、非選擇題另附評分準則**；每題寫完先過**試題檢核清單**——情境真實性、語句通順、資訊完整、問題明確、公平無偏見——通過才進下一步）
      - 題目若需要圖像輔助（看圖回答、量測、實驗裝置、幾何/座標/圖表、自然科圖示等），需明確規劃圖像；可參考課本/習作/教師手冊圖示裁切改標，或召喚 `draw` 技能產生符合題幹的新圖/改版圖。
   4. 整份查重（送 esa-exam-review，重複題只重寫該格，重寫後再查重，直到全過）
   5. 格式化輸出（套光復國小雙向細目表範本，輸出 Word 試題卷＋教師解答卷）
5. **題目篩選與替換**：老師對話指定題號要求換題 → 依原本那一格的規格（題型＋認知層次＋對應課次）重新出題 → 跑一次 Step 4 查重 → 通過後直接替換進同一份文件，不動其他配置。不支援整批調整。
6. **命題成果整理進 NotebookLM**：命題流程完成後，把這次的教材 PDF、試題卷、雙向細目表、查重報告，連同通用參考資料，整理進 NotebookLM 筆記本。
   - 每一次命題任務都建立**單獨的新 NotebookLM 筆記本**，不與其他學年度、年級、科目或次別混用。
   - 筆記本名稱使用「`學年度_年級_科目_次別_命題成果`」；若同名已存在，加上日期時間避免沿用舊本。
   - 通用參考資料（Bloom分類、命題原則、108領綱、素養導向文件、雙向細目表範本）與本次教材/成果都加入該任務專屬筆記本，讓每本筆記本能獨立使用。
   - 因任務已用獨立筆記本隔離，原則上不再依賴跨任務標籤分類；若使用標籤，只能在該任務筆記本內使用。
   - 若使用者的 NotebookLM 未連接/未登入，要詢問使用者是否要連接建立，或放棄這個動作，不能自己決定跳過或硬做。

## 輸出存放
新增「**命題成果**」資料夾，依「學年度_年級_科目_次別」建子資料夾，存放該次的試題卷、雙向細目表、設定摘要。

## 參考文件補充
- `定期命題參考架構/素養導向紙筆測驗要素與範例試題(定稿版).pdf`：國家教育研究院官方文件，素養導向命題兩大要素（真實情境佈題＋扣連學習表現/學習內容/核心素養），附國語文/英語文/數學範例試題。
- `定期命題參考架構/數學素養導向評量試題研發策略.docx`：數學科專屬補充，PISA 素養定義與情境四分類（個人/社會/職業/科學）、三種命題策略（從學習內容出發／從生活情境出發／從數學家建立知識角度出發）、題組題設計、試題檢核清單（含公平性/避免偏見檢查，已整合進 Step 3）。

## 資料夾結構
<!-- 初始化時自動掃描生成，之後新增檔案要更新 -->
```
定期考察命題技能/
├── agents.md（本檔）
├── handoff.md
├── .gitignore（已排除出版社教材、歷屆考古題兩個資料夾，皆有著作權疑慮且檔案過大）
├── skills/
│   └── exam-composer/
│       └── SKILL.md                （技能本體，2026-09-03 起納入版控＝單一真實來源；各機器從這裡同步到 ~/.claude、~/.codex、~/.agents）
├── 光復108課綱歷年定期試題/          （空——實測後發現 Step 4 不需要、也無法把 PDF 存進這裡，改用即時截圖比對，見「測試發現與修正」）
├── 命題範圍三家出版商教材/            （測試時已用真實康軒六上國語課本/習作/教冊資料篇/教學篇，約183MB，不進版控）
├── 命題成果/
│   ├── 115上_六年級_國語_第一次定期評量/   （測試產物：設定摘要.md、測試試題卷.docx、測試教師解答卷.docx）
│   ├── 115上_六年級_數學_第一次定期考察/   （正式成果：試卷、解答、細目表、查重報告、試題草稿、教材擷取、設定摘要、細目表骨架）
│   ├── 115上_六年級_自然_第一次定期考察/   （正式成果：試卷、解答、細目表、查重報告、試題草稿、教材擷取、設定摘要、細目表骨架。含 images/ 6幅Pillow智慧教材原圖裁切標註插圖、extracted_images/ 原始PDF提取插圖）
│   └── 115上_六年級_社會_第一次定期考察/   （正式成果：翰林版第一~二單元，正式試題卷.docx/教師解答卷.docx/雙向細目表.docx、查重報告.md、試題草稿.md、教材內容擷取.md、教材下載清單.md、設定摘要.md、雙向細目表骨架草案.md）
└── 定期命題參考架構/
    ├── 108領綱/
    │   ├── 108國語文領域領綱.pdf
    │   ├── 108數學領域領綱.pdf
    │   ├── 108社會領域領綱.pdf
    │   ├── 108自然領域領綱.pdf
    │   └── 108英語領域領綱.pdf
    ├── Bloom認知領域教育目標之認知歷程向度.docx
    ├── 新北市中和區光復國小定期評量雙向細目分析表.docx（雙向細目表範本，功能四應沿用此格式）
    ├── 紙筆測驗的命題原則.docx
    ├── 數學素養導向評量試題研發策略.docx（數學科素養導向命題策略補充）
    └── 素養導向紙筆測驗要素與範例試題(定稿版).pdf（國家教育研究院官方文件）
```

## 同步層級（本專案初始化至第 3 層級）

| 層級 | 平台 | 位置 | 讀取時機 |
|------|------|------|---------|
| L1 | 本地（GDrive） | `agents.md`＋`handoff.md` | 每個 session |
| L2 | GitHub | hsuyiping-rgb/exam-composer-skill（含 `skills/exam-composer/SKILL.md` 技能本體） | 指定時；**換電腦後安裝技能一律從這裡取** |
| L3 | Obsidian | 定期考察命題技能/專案工作流程.md | 有需要時 |

## 工作約定
- 任何 Agent、任何電腦：**開工先讀 `handoff.md`，收工必更新 `handoff.md`**
- 修改共用檔案前先讀最新內容，避免覆蓋其他 Agent 的變更
- 所有回應與文件使用繁體中文
- 修改前先確認計畫，優先保留原有資料結構
- 涉及出版社/校務系統帳密的操作：Agent 不得親自輸入使用者密碼（含透過腳本以環境變數方式提交登入表單），需由使用者本人在自己的瀏覽器完成登入，Agent 僅能讀取既有登入狀態或使用使用者提供的已授權深連結
- 命題生成流程（Step 1-5）必須完整執行，不能自行判斷跳過任何步驟；遇到不確定的狀況要暫停詢問使用者，不能自己猜答案繼續做
- 涉及 NotebookLM 等需要 OAuth 授權登入的服務：Agent 不得代使用者完成登入/授權流程，未連接時要詢問使用者是否要連接建立，或放棄該動作
- **技能本體的單一真實來源是 repo 的 `skills/exam-composer/SKILL.md`**；各機器從這裡同步到 `~/.gemini/config/skills/`（Antigravity 全域技能）、`~/.claude/skills/`、`~/.codex/skills/`、`~/.agents/skills/` 各處，修改後必須五份 SHA-256 一致並 commit，不可只改本機
- 本技能定位為「生成端」，與 `esa-exam-review`（校長使用的「審查端」，位於 `~/.codex/skills/esa-exam-review` 與 Claude Code 外掛 `anthropic-skills:esa-exam-review`）分工合作、不重疊
